---
phase: 05-polish-launch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Landing.tsx
  - src/components/landing/HeroSection.tsx
  - src/components/landing/ThemeShowcase.tsx
  - src/components/landing/PricingSection.tsx
  - src/components/landing/Footer.tsx
  - src/App.tsx
autonomous: false
---

<objective>
## Goal
Build a marketing landing page at the root URL (/) that sells Nest UI to GoHighLevel agencies. Includes hero section, theme showcase, pricing, and CTA — all driving visitors to sign up.

## Purpose
The landing page is the front door to nestui.io. Agencies land here, see what Nest UI does, view themes and pricing, and sign up. Without it, there's no way to acquire customers.

## Output
- Landing page at / with hero, theme showcase, pricing, and footer
- Public route (no auth required) — authenticated users redirect to /dashboard
- Responsive design matching Nest UI brand (dark, nestui-blue accents)
- Updated App.tsx routing: / shows Landing for unauthenticated, /dashboard for authenticated
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/App.tsx — Routing (catch-all currently redirects to /login or /dashboard)
@src/lib/themes.ts — THEMES array for showcase
@src/pages/Login.tsx — Existing auth page (landing CTA links here)
@src/pages/Billing.tsx — PricingTable pattern reference
</context>

<acceptance_criteria>

## AC-1: Hero Section
```gherkin
Given an unauthenticated visitor lands on /
When the page loads
Then they see a hero with headline, subheadline, and "Get Started" CTA button
And the CTA links to /login
And the hero conveys the value prop: rebrand GHL with premium themes
```

## AC-2: Theme Showcase
```gherkin
Given the landing page is loaded
When the visitor scrolls to the theme showcase section
Then they see a grid of theme cards showing all 9 themes
And each card shows the theme name, mode badge, swatch gradient, and description
And the section communicates variety and quality
```

## AC-3: Pricing Section
```gherkin
Given the landing page is loaded
When the visitor scrolls to pricing
Then they see 3 plan cards (Free, Pro, Agency) with prices and feature lists
And pricing matches PROJECT.md: Free $0, Pro $97/mo, Agency $399/mo
And each plan has a CTA button linking to /login
```

## AC-4: Routing Integration
```gherkin
Given an unauthenticated user visits /
When the page loads
Then the Landing page renders (not login, not dashboard)

Given an authenticated user visits /
When the page loads
Then they are redirected to /dashboard
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page components</name>
  <files>src/components/landing/HeroSection.tsx, src/components/landing/ThemeShowcase.tsx, src/components/landing/PricingSection.tsx, src/components/landing/Footer.tsx</files>
  <action>
    **HeroSection.tsx:**
    - Full-viewport-height section with centered content
    - Nest UI logo/name at top
    - Headline: bold, large — convey "Make GHL look like a $500K custom CRM"
    - Subheadline: 1-2 sentences explaining the product — one script tag, instant rebrand
    - Primary CTA: "Get Started Free" → Link to /login, nestui-blue button, large
    - Secondary CTA: "See Themes" → smooth scroll anchor to #themes section
    - Subtle background: gradient or radial glow effect using CSS (bg-nestui-bg base)
    - Badge above headline: "For GoHighLevel Agencies" pill

    **ThemeShowcase.tsx:**
    - Section with id="themes" for anchor scroll
    - Section heading: "9 Premium Themes" with subtext
    - Grid of 9 theme cards (3 columns on desktop, 1 on mobile)
    - Each card: swatch gradient background (theme.swatch), theme name, mode badge (dark/light), description, nav layout text
    - Import THEMES from src/lib/themes.ts — iterate over array
    - Cards have subtle hover effect (scale or border glow)

    **PricingSection.tsx:**
    - Section with id="pricing" for anchor scroll
    - Section heading: "Simple, Transparent Pricing"
    - 3 plan cards side by side (stack on mobile):
      - Free: $0/mo — 3 themes, 1 sub-account, no AI generator
      - Pro: $97/mo — All 9 themes, 5 sub-accounts, AI generator — "Most Popular" badge
      - Agency: $399/mo — All + custom, unlimited sub-accounts, AI generator, priority support
    - Each card: plan name, price, feature list with check/x icons, CTA "Get Started" → /login
    - Pro card visually highlighted (border-nestui-blue, scale slightly larger or ring)
    - Pricing matches PROJECT.md exactly

    **Footer.tsx:**
    - Simple footer: "© 2026 Nest UI. All rights reserved."
    - Links: Pricing (#pricing), Themes (#themes), Sign In (/login)
    - Subtle border-top, nestui-text3 text
  </action>
  <verify>npx tsc --noEmit — zero errors</verify>
  <done>AC-1, AC-2, AC-3 satisfied: Hero, theme showcase, and pricing components built</done>
</task>

<task type="auto">
  <name>Task 2: Create Landing page and update routing</name>
  <files>src/pages/Landing.tsx, src/App.tsx</files>
  <action>
    **Landing.tsx:**
    - Composes: HeroSection, ThemeShowcase, PricingSection, Footer
    - Single scrollable page, bg-nestui-bg
    - No auth required — fully public content
    - Smooth scroll behavior for anchor links (#themes, #pricing)

    **App.tsx:**
    - Import Landing page
    - Add route: "/" → show Landing if no session, redirect to /dashboard if session
    - Update catch-all: unauthenticated users go to "/" (not /login)
    - Keep /login route as-is for direct access
  </action>
  <verify>npx tsc --noEmit && npm run build — both pass</verify>
  <done>AC-4 satisfied: Root URL shows landing for visitors, redirects authenticated users to dashboard</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Marketing landing page at / with hero, 9-theme showcase, 3-tier pricing, and footer</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173/ (not logged in)
    3. Verify: Hero section with headline, subheadline, and "Get Started Free" CTA
    4. Click "See Themes" — smooth scroll to theme showcase
    5. Verify: 9 theme cards with swatch gradients, names, descriptions
    6. Scroll to pricing: 3 plans with correct prices ($0, $97, $399)
    7. Verify: Pro card has "Most Popular" highlight
    8. Click "Get Started" on any pricing card — goes to /login
    9. Verify: Footer with links
    10. Verify mobile responsive (375px width)
    11. If logged in: visiting / should redirect to /dashboard
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/lib/themes.ts — Read-only, import THEMES array
- src/pages/Login.tsx — Do not modify
- src/pages/Dashboard.tsx — Do not modify
- src/pages/Billing.tsx — Do not modify
- src/components/billing/* — Do not modify or reuse PricingTable (landing pricing is standalone)
- src/hooks/* — Do not modify any existing hooks
- supabase/functions/* — Do not modify

## SCOPE LIMITS
- No new npm dependencies
- No backend changes — landing page is purely frontend
- No analytics or tracking integration
- No blog, docs, or additional marketing pages
- Do not modify existing authenticated pages or components

</boundaries>

<verification>
Before declaring plan complete:
- [ ] npx tsc --noEmit — zero errors
- [ ] npm run build — succeeds
- [ ] / route shows landing page for unauthenticated visitors
- [ ] / redirects to /dashboard for authenticated users
- [ ] All 9 themes shown in showcase
- [ ] Pricing matches PROJECT.md ($0, $97, $399)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Landing page accessible at / with all sections rendering correctly
</success_criteria>

<output>
After completion, create `.paul/phases/05-polish-launch/05-04-SUMMARY.md`
</output>
